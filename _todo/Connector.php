<?php

namespace _todo;

class Connector
{
    /*
    public static function initRedis($config):mixed
    {
        $redis = new \Arris\Toolkit\RedisClient();

        $redis->connect($config['host'], $config['port'], $config['database'], $config['enable']);
    }
    */

}